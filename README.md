# cancelPayout_regressionTesting
Steps 1) npm install

Steps 2) go to RegressionTesting Directory and run mocha MainScript.js

For mocha reports

install : npm i -g mochawesome

mocha test.js --reporter mochawesome
